# Validation

Schema checks cover API version, kind, source cluster, and duplicate project names. Destination planning inventories principals and RoleTemplates, then classifies actions as create, remap, skip, unresolved, or missing. Compare logical binding tuples: principal type, principal, RoleTemplate, and service account—not generated object names.
